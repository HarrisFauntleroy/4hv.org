<% if @policy.destroy? %>
  <%= button_to t("delete"),
  @record,
  class: "btn",
  method: :delete,
  data: {
    turbo_confirm: t("common.confirmations.delete")
  },
  id: "delete_#{@record.model_name.singular}_button" %>
<% end %>
